# Card Game プロジェクトルール

## 命名規則

- **変数・引数・パラメータの省略禁止**: `s`, `m`, `v`, `e`, `p`, `b`, `a` などの1〜2文字の省略形を使わない
  - ❌ `const s = addLog(s, ...)` → ✅ `const state = addLog(state, ...)`
  - ❌ `function foo(e: Enemy)` → ✅ `function foo(enemy: Enemy)`
  - ❌ `.map((e, i) => ...)` → ✅ `.map((enemyState, index) => ...)`
  - 例外: ループカウンタの `i`, `j` は許容する

## 実装前の確認ルール

- 実装作業（コード追加・変更）を始める前に、**毎回ユーザーに「次に何を実装するか」を確認する**こと
- 計画立案フェーズや調査フェーズはこの限りではない（プランモード・Explore は確認不要）
- 確認の粒度: 「次のステップは X を実装します。よいですか?」レベルでよい
- 連続で複数の実装ステップを行う場合も、各ステップの直前に確認を入れる
- 例外: ユーザーが「全部一気にやって」「自動で進めて」と明示的に許可した場合のみスキップ可
